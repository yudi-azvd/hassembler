# Totalmente tirado de:
# https://github.com/actions/starter-workflows/blob/81295d6e715c29e03102adbe7d8e2f946a5fc8ac/ci/cmake.yml

name: Build

on:
  push:
    branches: [main,dev,github-actions]

env:
  BUILD_TYPE: Release


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Configure CMake
        working-directory: ${{github.workspace}}/build # cd build ??
        run: cmake .. -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

      - name: Build
        run: cmake --build . --config ${{env.BUILD_TYPE}}

      - name: Test
        run: ./utests


